import SectionTitle from "./SectionTitle";import ServiceCard from "./ServiceCard";import {services} from "../data/services";export default function Services(){return <section id="services" className="px-5 py-24"><div className="mx-auto max-w-7xl"><SectionTitle eyebrow="What we do" title="Software services built for modern business." description="From a business website to AI automation, every solution is designed around performance, usability and growth."/><div className="mt-14 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{services.map((s,i)=><ServiceCard key={s.title} service={s} index={i}/>)}</div></div></section>}